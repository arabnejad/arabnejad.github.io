http://jekyllrb.com/
 $ gem install jekyll bundler
~ $ jekyll new my-awesome-site
~ $ cd my-awesome-site
~/my-awesome-site $ bundle install
~/my-awesome-site $ bundle exec jekyll serve  --incremental
# => Now browse to http://localhost:4000

jekyll build


http://hadian.org/2014/09/15/how-to-build-an-academic-website-with-jekyll/



bundle exec jekyll build ; bundle exec jekyll serve  --incremental --watch ; pkill -KILL bundle

bundle exec jekyll build ; bundle exec jekyll serve  --incremental --watch ; pkill -KILL bundle; pkill -KILL jekyll


bundle exec jekyll build ; bundle exec jekyll serve  --incremental --watch ; pkill -KILL ruby2.3